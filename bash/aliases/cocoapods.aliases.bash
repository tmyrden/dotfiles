#!/bin/bash

# Some aliases for CocoaPods
alias p="pod"
alias pi="pod install"
alias pu="pod update"
alias po="pod outdated"
alias pli="pod list"
alias psrch="pod search"

pod-help () {
  echo "CocoaPods Alias Usage"
  echo
  echo "p     = pod"
  echo "pi    = pod install"
  echo "pu    = pod update"
  echo "po    = pod outdated"
  echo "pli   = pod list"
  echo "psrch = pod search"
  echo
}
